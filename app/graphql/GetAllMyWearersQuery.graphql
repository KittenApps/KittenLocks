query GetAllMyWearers($realmId: String)  {
  locks(myWearers: $realmId) @rest(type: "Lock", path: "/keyholder/wearers") {
    user @type(name: "User"){
      username
      avatarUrl
      discordUsername
    }
  }
}