#import "./LockFragment.graphql"

query GetMyWearers($status: String, $realmId: String) {
  locks(status: $status, myWearers: $realmId) @rest(type: "Lock", path: "/keyholder/wearers?status={args.status}") {
    ...LockFragment
  }
}